JF

1. cd ~/camera_ros2_ws
2. source install/setup.bash
3. cd src/spinnaker_camera_driver/launch
4. ros2 launch multiple_cameras.launch.py
5. Open another terminal window, and run rqt_image_view to see the video of the stereo cameras.

Yujie:

5.ros2 topic list | grep camera(this command is to let you doublecheck if the camera node is working)
6.If you want to use the ros2 calibration package, use this command:
ros2 run camera_calibration cameracalibrator --approximate 0.1 --size 10x7 --square 0.02 right:=/cam_0/image_raw left:=/cam_1/image_raw right_camera:=/cam_0/camera_info left_camera:=/cam_1/camera_info
Then you can flip the checkerboard until the x,y,size,skew lines become green
